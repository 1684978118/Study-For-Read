# Study For Read Phone - AI Development Rules

本项目采用 AI 辅助开发。任何 AI 在本目录工作前，必须先阅读并遵守本文件。

## 绝对禁止

- 禁止批量删除文件或目录。
- 不要使用：
  - `del /s`
  - `rd /s`
  - `rmdir /s`
  - `Remove-Item -Recurse`
  - `rm -rf`
- 需要删除文件时，只能一次删除一个明确路径的文件，例如：
  - `Remove-Item "D:\Codex\Study For Read Phone\path\file.txt"`
- 如果需要批量删除文件，应停止操作，并让用户手动删除。

## 项目边界

- 当前唯一新项目目录是：`D:\Codex\Study For Read Phone`。
- 旧项目 `D:\Codex\Study for Read` 只能作为只读参考，不允许修改。
- 未经用户明确同意，不允许在项目外创建、移动、删除、重命名文件。
- 不允许把旧项目代码直接复制到新项目；只能提炼需求、接口经验和业务思路。

## 开发顺序

1. 先写或更新规格文档。
2. 再写细颗粒度实施计划。
3. 再按单个任务卡执行。
4. 每个任务必须先写测试或验收检查，再写实现。
5. 每个任务结束必须运行验证命令，并把结果写入回复。

## 单任务约束

每次 AI 执行任务前，必须确认：

- 本次任务目标是什么。
- 本次允许修改哪些文件。
- 本次禁止修改哪些文件。
- 本次要先写哪些测试。
- 本次完成后运行哪些验证命令。

如果任务没有明确这些内容，AI 必须先补任务卡，不允许直接编码。

## 防止幻觉规则

- 不允许凭空假设目录、类名、接口名、数据库表名。
- 修改已有文件前必须先读取文件。
- 调用第三方库前必须确认依赖已存在；不存在时必须写入计划，不要偷偷引入。
- 不允许为了让测试通过而删除测试、放宽断言、跳过验证。
- 不允许修改与当前任务无关的文件。
- 如果发现计划和真实代码冲突，必须停止并说明冲突。

## 提交和验证

- 每个任务应尽量保持小提交粒度。
- 提交前必须运行该任务指定测试。
- 不能声称完成，除非刚刚运行过验证命令并看到通过结果。
- 如果工具链不可用，例如缺少 Flutter、JDK、Node，必须如实报告，不允许假装已验证。

