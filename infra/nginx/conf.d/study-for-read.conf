server {
    listen 80;
    server_name _;
    resolver 127.0.0.11 valid=30s ipv6=off;

    location = /health {
        alias /usr/share/nginx/health/health.html;
        default_type text/html;
    }

    location /api/ {
        set $api_upstream http://api:8080;
        proxy_pass $api_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /admin {
        return 301 /admin/;
    }

    location /admin/ {
        alias /usr/share/nginx/admin/;
        index index.html;
        try_files $uri $uri/ /admin/200.html;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /200.html;
    }
}

# HTTPS-ready placeholder. Enable in a later operational step after mounting
# certificate files at /etc/nginx/certs/fullchain.pem and /etc/nginx/certs/privkey.pem.
# No certificate or private key is committed by this repository.
#
# server {
#     listen 443 ssl;
#     server_name example.local;
#
#     ssl_certificate /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     location = /health {
#         alias /usr/share/nginx/health/health.html;
#         default_type text/html;
#     }
#
#     location /api/ {
#         set $api_upstream http://api:8080;
#         proxy_pass $api_upstream;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
#
#     location = /admin {
#         return 301 /admin/;
#     }
#
#     location /admin/ {
#         alias /usr/share/nginx/admin/;
#         index index.html;
#         try_files $uri $uri/ /admin/200.html;
#     }
#
#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri $uri/ /200.html;
#     }
# }
